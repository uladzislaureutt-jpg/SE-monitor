# S-monitor update 60

Build: `2026-08-27.social.60-result-event-integrity-1.7`

## База и причина

Пакет подготовлен поверх фактической сборки dry-run №14 (`github_sha`
`06c666dc6e7d9a33b0a4c6ffa6b1f8009cdf5f94`). Он сохраняет самостоятельное
масштабирование мягкого admission-хвоста и повышенные адресные лимиты из
актуального `config/settings.yaml`.

Dry-run №14 завершился устойчиво, но из 40 карточек содержал 12 подтверждённых
ложных включений и 7–8 необъединённых повторов. Материал о маленьком автобусе
вместо большого на маршруте Вилейка — Молодечно оставался риском ложного
отрицания.

## Result Integrity 1.7

- сохранены узкие варианты жалобы на автобус меньшей вместимости;
- исключены справочный материал о программистах 1С, кипрское административное
  послабление, китайский отзыв Geely без связи с белорусским рынком, частная
  продажа старого Kinder, общее заявление о борьбе с импортом, совет о болотных
  фотосессиях, нейтральные правила соцучреждений, обычное спецпроизводство,
  частный DIY-сюжет, положительная динамика зарплат относительно инфляции и
  многосюжетный вечерний дайджест;
- правила требуют точных жанровых сочетаний и не являются общими запретами по
  словам `правила`, `жалоба`, `качество`, `ремонт` или `один случай`;
- исправлены категории жалоб дольщиков, доступа к гинекологу, контейнерной
  площадки, статистики незанятых, торгового норматива МАРТ и коммунальных жалоб
  на выездном приёме.

## Event Integrity 1.7

Добавлены консервативные семантические семьи для:

- шоколада `Чаржед`;
- спрея для ног с борной кислотой;
- какао-порошка;
- статистики занятости и базы незанятых;
- расходов КГК на мост и путепровод;
- обмеления одной и той же именованной реки;
- кипрского ВНЖ с просроченными паспортами как защитного сценария, хотя такой
  материал теперь отсекается Result Integrity.

Разные реки, разные товары, разные регионы, продолжения события и аналитические
материалы не объединяются.

## Состав пакета

- `social_monitor.py` — заменить в корне;
- `config/settings.yaml` — заменить, чтобы сохранить актуальные мягкие лимиты;
- `tests/test_result_event_integrity_17_report_14.py` — добавить;
- `UPDATE_MANIFEST.md` — заменить;
- `MANIFEST.sha256` — заменить.

Все файлы должны попасть в один commit.

## Не изменяется

- список источников и `config/sources.csv`;
- глобальный предел, расширенные локальные лимиты и Soft Admission/Core 3.3;
- workflow, расписание, timeout и concurrency;
- state, discovery cache и known URLs;
- email, Telegram, секреты и доставка;
- Content Access Recovery и SetFit.

## Проверка

- целевые тесты отчёта №14: `5 passed`;
- полный набор репозитория: `393 passed`;
- replay итоговых карточек №14: 13 подтверждённых исключений, 27 сохранённых
  публикаций и 20 карточек после консолидации; это диагностический replay, а не
  замена нового dry-run;
- Python compile и синтаксис тестов: `PASSED`;
- контрольные суммы пакета: должны совпасть полностью.

После установки выполнить один dry-run. Ожидаемая сборка:

`2026-08-27.social.60-result-event-integrity-1.7`

## Критерии успеха

- workflow проходит полный набор тестов;
- state/cache не изменяются в dry-run;
- сюжет о недостаточной вместимости автобуса сохраняется;
- подтверждённые ложные жанры №14 не возвращаются;
- повторы по одному товару, объекту, показателю или именованной реке выводятся
  одной основной карточкой со строкой «Этот сюжет также освещали»;
- `clipped_protected_title` остаётся равным нулю.

## Откат

Вернуть предыдущие версии `social_monitor.py`, `config/settings.yaml`,
`UPDATE_MANIFEST.md` и `MANIFEST.sha256`, удалить тест 1.7 одним commit.
State/cache откатывать не требуется после dry-run.
